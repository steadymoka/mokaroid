<manifest
	xmlns:android="http://schemas.android.com/apk/res/android"
	package="moka.land.fileutil">

	<application>
		<provider
			android:name="._FileUtil"
			android:authorities="${applicationId}.fileutilprovider"
			android:exported="false" />
	</application>

</manifest>
