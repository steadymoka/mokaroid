<manifest
	xmlns:android="http://schemas.android.com/apk/res/android"
	package="moka.land.transcoder">

	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

	<application>

		<provider
			android:name="._Transcoder"
			android:authorities="${applicationId}.transcoderprovider"
			android:exported="false" />

	</application>

</manifest>
